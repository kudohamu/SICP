(define (double a) (+ a a))
(define (halve a) (/ a 2))
(define (even? n) (= (remainder n 2) 0))
(define (fast-multi a b)
    (cond ((= b 0) a)
        ((even? a) (double (fast-multi a (halve b))))
        (else (+ a (fast-multi a (b - 1))))
    )
)
